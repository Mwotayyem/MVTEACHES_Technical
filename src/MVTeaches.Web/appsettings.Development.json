{
  "DetailedErrors": true,
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "_comment_LocalDevelopmentSeed": "Local-only F5 bootstrap (auto-migrate + idempotent dummy accounts/content) — see docs/LOCAL-DEVELOPMENT.md. Safe non-secret defaults only: Enabled stays false until you turn it on yourself, and RequiredDatabaseName is a guard, not a secret. SeedPassword is NOT set here — it must come from User Secrets, never this committed file.",
  "LocalDevelopmentSeed": {
    "Enabled": false,
    "RequiredDatabaseName": "mvteaches_local"
  }
}
