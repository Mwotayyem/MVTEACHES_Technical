@page
@model MVTeaches.Web.Pages.Files.ReceiptModel
@*
    This page never renders — OnGetAsync always returns a FileResult or
    NotFound(). The markup below is unreachable in practice; it exists only
    because Razor Pages requires a .cshtml file to establish the route.
*@
