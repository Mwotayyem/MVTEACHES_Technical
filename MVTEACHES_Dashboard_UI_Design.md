# MVTEACHES — تصميم واجهة لوحة التحكم المرئية (Visual CRM Dashboard)
### UI/UX Design Brief — v1.2

| | |
|---|---|
| **الإصدار** | v1.2 — 🔴 **تصحيح جوهري: أُزيلت "Next payment"، لا تقسيط إطلاقًا** · ⭐ **مُواءَمة مع `D-83` (الحضور بضغطة Join)** |
| **التاريخ** | 2026-08-25 · مُحدَّثة 2026-08-25 (`D-83`) |
| **الحالة** | ✅ **معتمد كهدف لواجهة الـMVP** — بقرار صريح من المالك، **يحل محل** قرار "تقارير مالية أساسية فقط، لا نظام BI" الوارد سابقًا **لخصوص واجهة الأدمن** · ⭐ **الأربعة قرارات المعلّقة (§14) حُسمت كلها 2026-08-25** |
| **المصدر** | رؤية المالك الكاملة (2026-08-25): سجل طلاب مرئي · ملف طالب · متتبع دفعات · عدّاد دورة · لوحة رئيسية · قسم دورات |
| **العلاقة بالدراسة الأم** | هذه الوثيقة **طبقة عرض فقط (Presentation Layer)**. لا تُضيف جدولًا واحدًا ولا منطق أعمال جديدًا إلى [`MVTEACHES_Technical_Study_v2.md`](MVTEACHES_Technical_Study_v2.md) — كل رقم ولون ومؤشر هنا **مُشتق من كيانات موجودة أصلًا** في القرارات D-01…D-66 + D-83 |
| **قرار مرتبط لم يُتَّخذ الآن** | إضافة أحداث تنبيه جديدة (دورة اكتملت، طالب جديد يبدأ غدًا) — **أُجِّلت بقرار المالك**، تبقى مصفوفة الإشعارات في §30 من الدراسة الأم كما هي |

---

## 1. المبدأ العام

> **ليس Excel. Canva + بوابة مدرسية حديثة + CRM بسيط.**

لوحات ملوّنة · صور رمزية للطلاب (Avatars) · أشرطة تقدّم · مؤشرات دفع بصرية · عدّادات تنازلية للدورات. **تصميم أول للموبايل (Mobile-First)** — الافتراض التشغيلي أن الأدمن يتابع الطلاب والدفعات ومواعيد الدورات من الهاتف أكثر من اللابتوب.

**قاعدة حاكمة واحدة تحكم هذه الوثيقة بالكامل:**

> **كل رقم أو لون معروض هنا يُحسب لحظيًا من بيانات موجودة (الدفتر · المدفوعات · الاشتراكات · تحقق التسليم) — لا حقل جديد يُخزَّن لمجرد العرض.** هذا امتداد مباشر لمبدأ الدراسة الأم: "الرصيد مشتق دائمًا، لا مخزّن أبدًا" (§20.2).

---

## 2. نظام الألوان والحالات — Status Color System

### 2.1 الحالات الست ومصدر كل واحدة

| الحالة | اللون | شرط الحساب | المصدر (بلا حقل جديد) |
|---|---|---|---|
| **بدء قريبًا** | 🔵 Starting soon | `subscription.starts_on` خلال `StartingSoonDays` القادمة، ولم يبدأ بعد | `subscriptions.starts_on` |
| **نشط** | 🟢 Active | `subscription.status = Active` ولا يوجد مستحق ولا اقتراب انتهاء | `subscriptions.status` + معادلة المستحق (§22.1) |
| **دفعة مستحقة** | 🟠 Payment due | `المستحق > 0` (السعر − مجموع المدفوعات المؤكدة) | معادلة §22.1 — **بلا عمود `outstanding` مخزَّن** |
| **الدورة تنتهي قريبًا** | 🔴 Ending soon | `subscription.expires_on` خلال `EndingSoonDays` القادمة، أو الدقائق المتبقية في الدفتر أقل من حصتين | `subscriptions.expires_on` + `SUM(entitlement_ledger.delta_minutes)` |
| **مكتمل** | ⚪ Completed | `subscription.status = Completed` | `subscriptions.status` |
| **غير نشط** | ⚫ Inactive | `subscription.status IN (Expired, Cancelled, Suspended)` ولا اشتراك لاحق نشط | `subscriptions.status` |

### 2.2 ✅ ترتيب الأولوية عند تعارض أكثر من حالة — معتمد

مثال المالك نفسه أوضح المشكلة: **ماريا نشطة فعلًا (منتصف الدورة) لكن لديها مستحق، فظهرت كـ"Payment due" لا "Active".** هذا يعني وجود **ترتيب أولوية** بين الحالات الست، وليس شرطًا مستقلًا لكل واحدة.

**الترتيب المعتمد (2026-08-25) — الأكثر إلحاحًا يفوز:**

```
1. 🔴 Ending soon      ← الأخطر ماليًا وتشغيليًا (خطر فقدان ساعات مدفوعة)
2. 🟠 Payment due       ← يمنع الحضور فعليًا (D-14)
3. 🔵 Starting soon     ← إعلامي، لا خطر
4. 🟢 Active            ← الوضع الطبيعي
5. ⚪ Completed
6. ⚫ Inactive
```

### 2.3 ✅ العتبات الزمنية — إعدادات أدمن لا أرقام ثابتة

اتساقًا مع `D-65` (كل ثابت عمل يضبطه الأدمن)، **أعداد الأيام في "قريبًا" لا تُكتب في الكود**. تُضاف ثلاثة مفاتيح جديدة إلى جدول `settings` (امتداد لجدول §19.5 الموجود، لا جدول جديد) — **القيم الافتراضية اعتُمدت 2026-08-25، وتبقى قابلة للتعديل من لوحة الأدمن دون نشر إصدار جديد**:

| المفتاح الجديد | القيمة الافتراضية المعتمدة | يتحكم في |
|---|---|---|
| `StartingSoonDays` | **7** | 🔵 عتبة "بدء قريبًا" |
| `EndingSoonDays` | **5** | 🔴 عتبة "الدورة تنتهي قريبًا" |
| `LowRemainingLessonsThreshold` | **2** | 🔴 عتبة "ساعات متبقية قليلة" (بديل/مكمّل لعتبة التاريخ) |

---

## 3. صفحة سجل الطلاب الرئيسية — Students Register

جدول/بطاقات هجينة (جدول على الشاشات الكبيرة، بطاقات على الموبايل — نفس البيانات، عرض مختلف).

| العمود المعروض | المصدر الفعلي |
|---|---|
| اسم الطالب + صورة رمزية | `students.name` (+ حرف أول كـ Avatar افتراضي — **لا صور فعلية للأطفال في MVP**، اتساقًا مع قاعدة الخصوصية §32.5 بند 4) |
| الدورة | `courses.name` + `levels.code` عبر `subscriptions` النشط |
| تاريخ البدء / الانتهاء | `subscriptions.starts_on` / `expires_on` |
| المدفوع | `SUM(payments.amount) WHERE status='Confirmed'` |
| المتبقي (ماليًا) | معادلة §22.1: `price_amount − المدفوع` |
| التقدّم (شريط) | ✅ **تقدّم الباقة/الكورس الحالي** — `الدقائق المستهلكة ÷ minutes_total` للاشتراك الحالي (معتمد 2026-08-25) |
| الحالة (لون) | القسم 2 أعلاه |

> ✅ **معتمد:** السجل الرئيسي يعرض **تقدّم الباقة الحالية فقط** (ما يهم التشغيل اليومي). **تقدّم المستوى نحو الشهادة** (`level_progress` ÷ `settings.CertificateRequiredHours`) يظهر **حصريًا داخل الملف الشخصي** (§4) — لا يتكرر في السجل الرئيسي.

**الفلاتر المتوقعة:** الحالة (الألوان الست) · الدورة · المستوى · نطاق تاريخ الانتهاء — كلها استعلامات على أعمدة موجودة، لا بنية جديدة.

---

## 4. صفحة الملف الشخصي للطالب — Student Profile

| القسم | الحقل المعروض | المصدر |
|---|---|---|
| **الترويسة** | الاسم · الدورة · المستوى الحالي | `students` + `student_levels (is_current)` |
| **تقدّم الدورة** | X / Y حصة مكتملة · دائرة % | ⭐ `SUM(entitlement_ledger.delta_minutes) WHERE reason='Consumption'` لهذا الاشتراك ÷ `minutes_total` — **مصدره ضغطة Join لا تحقق التسليم** (`D-83`) |
| **💰 الدفع** | سعر الدورة · المدفوع · المتبقي · حالة الدفع | `subscriptions.price_amount` + دفتر المدفوعات (§22.1) |
| **💰 تاريخ انتهاء الباقة (Package End Date)** | تاريخ | `subscriptions.expires_on` — ✅ **لا علاقة له بأي قسط** (انظر §5) |
| **📅 الدورة** | البدء · الانتهاء · المدة · حصص متبقية | `subscriptions` + `SUM(entitlement_ledger.delta_minutes) ÷ duration_minutes` |
| **📚 التعلّم** | المستوى الحالي/المستهدف · حصص مكتملة · الحضور · المعلم · ملاحظات | `student_levels` + `attendance` (⭐ الغياب **مُشتقّ**: حصة مسجَّل فيها بلا صف Join — `D-83`) + `class_sessions.teacher_id` + `homework` |
| **الواجبات** | حالة كل واجب | `homework_submissions` |

---

## 5. ✅ التقسيط ملغى نهائيًا — لا "دفعة قادمة"، بل "تاريخ انتهاء الباقة"

**تأكيد صريح من المالك (2026-08-25):** الطالب يدفع سعر الباقة **كاملًا**، ولا يوجد نظام أقساط ولا دفعات مستقبلية داخل الباقة — يبقى `D-38` (لا تقسيط) ساريًا بلا استثناء.

➜ **`Next payment` أُزيلت نهائيًا من كل شاشات هذا التصميم**، واستُبدلت بـ **"Package End Date" / "تاريخ انتهاء الباقة"** — وهي `subscriptions.expires_on` نفسها، المعروضة أصلًا في قسم "📅 الدورة". **لا حقل جديد ولا جدول جديد ولا منطق أعمال جديد.** هذا تصحيح تسمية بحت طُبِّق في §4 أعلاه.

---

## 6. متتبع المدفوعات — Payment History

عرض مباشر لجدول `payments` الموجود أصلًا (§22.1)، مرتّبًا بالتاريخ:

```
1 Aug   — £100 — ✅ Paid       ← payments WHERE status='Confirmed'
15 Aug  — £100 — ✅ Paid
1 Sept  — £100 — 🟠 Due        ← لا صف بعد في `payments` لهذه الباقة — لا علاقة بأي قسط (§5)
```

**لا تغيير على الجدول.** فقط عرض تاريخي لسجل موجود.

---

## 7. عدّاد الدورة — Course Countdown

```
🟢 12 days remaining          ← expires_on − اليوم
Course ends 30 August         ← subscriptions.expires_on
18 / 24 lessons completed     ← دقائق مُسلَّمة ÷ minutes_total (بالحصص لا بالدقائق للعرض فقط)
دائرة تقدّم 75%                ← نفس الحساب أعلاه
```

اللون في العدّاد يتبع نفس عتبة `EndingSoonDays` من §2.3 — **لا منطق لون مستقل ثانٍ**.

---

## 8. لوحة التحكم الرئيسية — Main Dashboard

> ⚠️ **هذا هو القسم الذي كان مستبعدًا صراحةً من المالك سابقًا** ("لا أريد نظام BI أو شاشة تحليلات متقدمة")، **والآن معتمد بقراره الصريح كهدف MVP.** جميع الأرقام أدناه **مشتقة من الدفتر، لا عدّادات مخزَّنة** — نفس مبدأ §37.4 من الدراسة الأم.

| البطاقة | المصدر |
|---|---|
| 👩‍🎓 عدد الطلاب النشطين | `COUNT(students) WHERE current subscription.status='Active'` |
| 💰 المحصَّل هذا الشهر | `SUM(payments.amount) WHERE status='Confirmed' AND MONTH=current` |
| 🟠 المستحق الإجمالي | `SUM` معادلة §22.1 عبر كل الطلاب |
| 📚 عدد الدورات الجارية | `COUNT(DISTINCT course_id) WHERE subscriptions.status='Active'` |
| ⏰ دورات تنتهي هذا الأسبوع | `COUNT WHERE expires_on BETWEEN now AND now+7` |

**"يحتاجون انتباهًا" (Needs Attention):** قائمة مُصفّاة من نفس استعلامات §2.1 — 🔴 قائمة `Ending soon` · 🟠 قائمة `Payment due` · 🔵 قائمة `Starting soon`. **لا استعلام جديد — نفس بيانات السجل الرئيسي، مُعاد تجميعها.**

**التحية الشخصية ("Good evening, Maria"):** من `users.name` لجلسة الأدمن الحالية — تجميلي بحت.

---

## 9. قسم الدورات — Courses Section

بطاقة لكل دورة نشطة تعرض: عدد الطلاب المسجَّلين (`COUNT(subscriptions) WHERE course_id=X AND status='Active'`) · السعر (`pricing_plans`) · المدة (`validity_days`). النقر يفتح قائمة مُفلترة من §3 بنفس `course_id`. **لا كيان جديد** — `courses` موجود أصلًا (§13).

---

## 10. التنبيهات — بلا تغيير

بقرار المالك، **لا تُضاف أحداث تنبيه جديدة الآن.** مصفوفة الإشعارات في §30 من الدراسة الأم (تذكير دفعة مستحقة، اقتراب انتهاء اشتراك، …) تبقى كما هي، وهي **بالفعل تغذّي معظم ما يظهر في "يحتاجون انتباهًا" أعلاه** دون أي إضافة.

---

## 11. متطلبات الموبايل

- **Mobile-first** فعليًا لا "متجاوب لاحقًا" — الافتراض أن أغلب استخدام الأدمن اليومي من الهاتف.
- بطاقات (Cards) هي العرض الافتراضي على الشاشات الصغيرة؛ الجدول التقليدي (§3) خيار للشاشات الكبيرة فقط.
- كل شريط تقدّم ودائرة تقدّم يجب أن تُقرأ بوضوح بعرض هاتف قياسي (~375px) بلا تمرير أفقي.

---

## 12. ما لا يتغيّر — تأكيد صريح

| البند | الحالة |
|---|---|
| جداول قاعدة البيانات | ❌ **لا جدول جديد** — كل شيء في هذه الوثيقة عرض لبيانات §33.2 الموجودة |
| منطق العمل (D-01…D-66 + D-83) | ❌ **بلا تغيير** — هذه طبقة عرض فقط |
| القرارات المالية (الدفتر، Snapshot، لا تقسيط) | ❌ **بلا تغيير** |
| الاستثناء الوحيد | ✅ إضافة 3 مفاتيح إلى `settings` (§2.3) — امتداد لنمط `D-65` الموجود، لا بنية جديدة |

---

## 13. الأثر على الجهد

هذه الوثيقة **تزيد جهد بناء واجهة الأدمن** (لوحة الإدارة + التقارير، بند §38.1 المقدَّر بـ 12 يومًا) بشكل ملموس — رسوم بيانية، أشرطة تقدّم، بطاقات ملوّنة، عرض متجاوب للموبايل أثقل من جداول HTML بسيطة. **لا رقم أخترعه هنا** — إعادة تقدير هذا البند تحديدًا (لا المشروع كله) مطلوبة عند بدء Phase 11 (لوحة الإدارة والتقارير)، بعد استقرار تصميم الشاشات الفعلي (Mockups).

---

## 14. القرارات المطلوبة من المالك — ✅ كلها حُسمت 2026-08-25

| # | القرار | الحسم |
|---|---|---|
| ~~1~~ | ~~ترتيب أولوية الحالات الست عند التعارض~~ | ✅ الترتيب المقترح في §2.2 اعتُمد كما هو حرفيًا |
| ~~2~~ | ~~قيم عتبات `StartingSoonDays` / `EndingSoonDays` / `LowRemainingLessonsThreshold`~~ | ✅ اعتُمدت: 7 · 5 · 2 على التوالي (§2.3) — قابلة للتعديل من لوحة الأدمن لاحقًا |
| ~~3~~ | ~~أي تقدّم يُعرض في السجل الرئيسي~~ | ✅ تقدّم الباقة الحالية في السجل الرئيسي (§3) · تقدّم المستوى/الشهادة حصريًا في الملف الشخصي (§4) |
| ~~4~~ | ~~"الدفعة القادمة" مقابل قسط جديد~~ | ✅ **لا تقسيط إطلاقًا، `D-38` يبقى كما هو** — الحقل أُعيدت تسميته "تاريخ انتهاء الباقة / Package End Date" (§5) |

**لا قرارات مفتوحة متبقية في هذا التصميم.** جاهز للانتقال إلى مرحلة الـMockups الفعلية.
